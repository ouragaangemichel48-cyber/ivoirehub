# .env
# Fichier utilisé en LOCAL par netlify-cli (netlify dev)
# NE JAMAIS committer ce fichier une fois rempli avec les vraies valeurs !
# Ajoute-le à ton .gitignore.
#
# En production (Netlify), ces mêmes variables doivent être définies dans :
# Site settings > Environment variables (PAS dans ce fichier)

DB_HOST=sqlXXX.infinityfree.com
DB_USER=if0_12345678
DB_PASS=MON_MOT_DE_PASSE
DB_NAME=if0_12345678_nomdb
